[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lxsrs_v2"
version = "0.1.0"
dependencies = ["numpy", "sounddevice", "pynput", "opuslib"]

[tool.setuptools.package-data]
lxsrs_v2 = ["AudioEffects/**/*.wav", "AudioEffects/*.wav"]