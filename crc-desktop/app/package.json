{
  "name": "crc-server",
  "version": "0.1.0",
  "description": "crc-desktop's local server: static hosting + on-demand crc-sync proxies + local SRS radio bridge",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}
